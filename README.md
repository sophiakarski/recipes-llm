# recipes-llm

(To clean up)

1. GitHub repo and clone
2. uv init
3. Documentation: uv add openai (from page)
4. Put in main.py file the Get Started Python code
5. Create .env file (so that you don't have to run 'export OPENAI_API_KEY="your_api_key_here"' every time) -> 'your_api_key_here' is secret key
6. Make sure code is using default environment variable (dotenv)
7. uv add python-dotenv (module)
8. Import dotenv package in main.py
9. Run main.py -> it works

10. uv add "fastapi[standard]"
11. get code from FastAPI docs
12. Changes to main.py
13. fastapi run
(ps aux | grep python)

<!-- System Prompt -->

<!-- package, library (dependencies), module [e.g. openai is a python module -> sdk - allow us to do things in an easier way] -->